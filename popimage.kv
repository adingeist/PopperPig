# stylizes the pop image icon
<PopImage>:
    canvas.before:
        Rectangle:
            # creates a rectangle for the pop image image
            size: self.myWidth, self.myHeight
            pos: self.position_x, self.position_y
            source: "Assets/pop.png"
